const { validate } = require("../models/users");

// test("validate request", () => {
//   const result = validate({ lastName: "afgdg" });
//   expect(result).toThrow();
// });

describe('/api.users', ()=> {
    describe
} )
